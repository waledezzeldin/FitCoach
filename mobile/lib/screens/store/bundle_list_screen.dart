import 'package:flutter/material.dart';

class BundleListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Replace with actual bundle data fetching and UI
    return Scaffold(
      appBar: AppBar(title: Text('Coaching Bundles')),
      body: Center(child: Text('List of bundles will appear here.')),
    );
  }
}